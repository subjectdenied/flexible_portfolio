// Frontend bundle — Divi's built-in JS handles portfolio filtering
// via the et_pb_filterable_portfolio class. This file is intentionally
// minimal as we reuse Divi's filtering JS.
// Note: must produce non-empty output so wp_enqueue_script registers it
// as a valid dependency for the builder bundle.
window.__flexiblePortfolio = true;
